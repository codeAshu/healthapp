<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iiitb.healthapp"
    android:versionCode="1"
    android:versionName="1.0" >

    
    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="17" />

     <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
     The following two permissions are not required to use
     Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    
    <application
        android:name=".HealthApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.iiitb.healthapp.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
   <!--  Entry for loginActivity.class -->
        <activity android:name=".LoginUserActivity"
                  android:label="login to your Account"></activity>
        
        <!--  Entry for RegisterActivity.class -->
        <activity android:name=".RegisterUserActivity"
                  android:label="Register New Account"></activity>
        
        <!--  Entry for loginActivity.class -->
        <activity android:name=".LoginDocActivity"
                  android:label="login to your Account"></activity>
        
        <!--  Entry for RegisterActivity.class -->
        <activity android:name=".RegisterDocActivity"
                  android:label="Register New Account"></activity>
         
         <!--  Entry for DocViewFamilyActivity.class -->
        <activity android:name=".DocViewFamilyActivity"
                  android:label="Registerd Families"></activity>
       
          <!--  Entry for NewVisitActivity.class -->
        <activity android:name=".NewVisitActivity"
                  android:label="Add the visit details"></activity>
       
        
        <!--  Entry for ViewMemberActivity.class -->
        <activity android:name=".ViewMemActivity"
                  android:label="View the Members of the family"></activity>
        
         <!--  Entry for MemberActivity.class -->
        <activity android:name=".MemberActivity"
                  android:label="Add the Members of the family"></activity>
        
        
          <!--  Entry for AddMemberActivity.class -->
        <activity android:name=".AddMemberActivity"
                  android:label="Add the Members of the family"></activity>
        
         <!--  Entry for AddDoctorActivity.class -->
        <activity android:name=".AddDoctorActivity"
                  android:label="Add the Doctor of the family"></activity>
        
        
        
        <!--  Entry for dummy.class -->
        <activity android:name=".DashboardActivity"
                  android:label="dummy"></activity>
    </application>

</manifest>
